dash>=2.14.0
numpy>=1.24.0
pandas>=2.0.0
plotly>=5.18.0
Pillow>=10.0.0
scipy>=1.11.0
scikit-image>=0.21.0

# dashboard.py uses Dash/plotly/scipy/scikit-image (above).

# annotate_server.py — FastAPI + HTMX image annotation tool (CPU is fine for the PoC).
# In the 3dmodel conda env these are already installed (torch 2.7.0+cpu).
torch>=2.2
torchvision>=0.17
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1
python-multipart>=0.0.9
a2wsgi>=1.10          # mounts the Dash dashboard.py under FastAPI (Dashboard tab)
